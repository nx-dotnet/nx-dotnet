{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "@nx-dotnet/core:swagger-typescript",
  "title": "Swagger Typescript Generator",
  "description": "Generates typescript code based on a specified openapi/swagger json file",
  "type": "object",
  "properties": {
    "outputProject": {
      "type": "string",
      "description": "Which project should the typescript be generated inside of?"
    },
    "openapiJsonPath": {
      "type": "string",
      "description": "Path to the openapi specification file.",
      "aliases": ["swaggerJsonPath"]
    },
    "templateDirectory": {
      "type": "string",
      "description": "Used to override the templates that are used when generating the typescript"
    }
  },
  "required": ["outputProject", "openapiJsonPath"]
}
